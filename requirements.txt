# Runtime/static deploy dependencies.
#
# This dashboard is deployed as a static site, so the default requirements file
# is intentionally empty. Keep Python-only research dependencies in a more
# specific file so static hosts that run `pip install -r requirements.txt` do
# not install unnecessary packages.
